#ifndef DECODER
#define DECODER

/**
 * Looks for a plain text file named 'filename' and processes it to print the encoded
 * values
 * @param filename
 */
void print_text(char filename[]);

/**
 * Looks for a binary file named 'filename' and processes it to print the encoded values
 * @param filename
 */
void print_binary(char filename[]);

#endif // DECODER
